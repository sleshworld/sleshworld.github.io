# Психологические практики

Локальный инструмент для заполнения заданий и ведения постоянных списков:

- диагностика убеждений и стратегий;
- дневник стрессовой ситуации;
- рационализация после дневника, когда действие снимает чувство, а не решает реальную проблему;
- подробный сценарий экспозиции с маршрутом и перечнем исключаемых действий;
- сворачиваемый отчёт внутри сценария с хронологией и динамикой чувства;
- постоянный список охранительных, избегающих и адаптивных действий;
- постоянный список триггеров с оценкой сложности для психики в процентах.

## Как открыть

На сервере: https://stress-diary-45-145-163-147.sslip.io/

Открой `index.html` в браузере.

## GitHub Pages

Сайт можно опубликовать через GitHub Pages из корня репозитория:

- repository: public;
- branch: `main`;
- folder: `/root`.

После публикации GitHub выдаст адрес вида `https://username.github.io/stress-diary-app/`.

Важно: записи привязаны к конкретному адресу сайта в конкретном браузере. Если перейти с VPS-адреса на GitHub Pages, старые записи не перенесутся автоматически. Перед переездом лучше сохранить важные записи в PDF или скачать резервную копию JSON.

## Вставка в Tilda

Основной вариант состоит из трёх читаемых блоков:

1. Создай в Tilda отдельную страницу и добавь подряд три блока `T123` из категории `Другое`.
2. В первый вставь `TILDA-BLOCK-1-INTERFACE.txt` - HTML и CSS интерфейса.
3. Во второй вставь `TILDA-BLOCK-2-PRACTICES.txt` - вопросы и структуру практик.
4. В третий вставь `TILDA-BLOCK-3-APPLICATION.txt` - логику приложения.
5. Сохрани блоки и опубликуй страницу. В редакторе Tilda код может отображаться как текст; приложение запускается на опубликованной странице.

Все три файла читаемые и остаются ниже безопасного ограничения размера. Они имеют общий номер версии: если забыть один блок или смешать разные сборки, на странице появится понятное сообщение. При обновлении нужно заменять все три блока.

Компактный одноблочный вариант сохранён в `TILDA-COPY-PASTE.txt` как запасной. Он сжат и распаковывается браузером при открытии страницы. `tilda-embed.html` и `tilda-multi-embed.html` нужны для автоматической проверки обоих вариантов.

Ни один вариант не загружает JavaScript, CSS или шрифты с GitHub и других сайтов. Интерфейс изолирован от стилей Tilda, а записи используют отдельные ключи `psychological-practices.*` в локальном хранилище браузера. При обновлении старого тестового Tilda-блока записи из `mdpsy.stress-diary.*` переносятся автоматически.

После изменений в `index.html`, `styles.css` или `app.js` пересобери блок:

```powershell
node .\build-tilda-embed.cjs
```

Для обновления сервера:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\deploy.ps1
```

## Где хранятся данные

Записи хранятся в `localStorage` конкретного браузера и конкретного пути к файлу.

Практически это значит:

- данные не отправляются на сервер;
- пустой дневник, пустая диагностика и пустые строки списков не сохраняются;
- в другом браузере записей не будет;
- если очистить данные сайта в браузере, записи пропадут;
- кнопка `Скачать резервную копию` сохраняет записи, действия и триггеры в JSON;
- после каждых трёх новых записей приложение напоминает скачать JSON; после скачивания отсчёт начинается заново.

При каждом открытии приложение показывает главную с выбором практики и последними записями. При первом посещении оно кратко объясняет локальное хранение, а постоянный блок над формой напоминает о форматах MD, PDF и JSON.

## Экспорт для психолога

Внутри записи доступны:

- `Скопировать` - текст в буфер обмена;
- `Markdown` - файл с текущей записью;
- `Сохранить PDF` - открывает печать браузера; в списке принтеров выбери "Сохранить как PDF".

На macOS в системном окне печати открой меню `PDF` внизу и выбери `Save as PDF` / `Сохранить как PDF`.

PDF-экспорт оформлен как отчет и скрывает пустые поля.
Постоянные списки действий и триггеров сохраняются как таблицы в альбомной ориентации; их Markdown-экспорт также имеет табличный формат.

## Проверка

Smoke-test запускается через Node.js и системный Chrome/Edge:

```powershell
$env:NODE_PATH='C:\Users\slesh\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules;C:\Users\slesh\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\node_modules\.pnpm\node_modules'
& 'C:\Users\slesh\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' '.\smoke-test.cjs'
```

Для проверки Tilda-версии используй `tilda-smoke-test.cjs` с тем же `NODE_PATH`.
